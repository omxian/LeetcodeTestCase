"""
        >>> root = TreeNode(4)
        >>> root.left = TreeNode(2)
        >>> root.right = TreeNode(7)
        >>> Solution().searchBST(root, 2).val
        2
        >>> root = TreeNode(4)
        >>> root.left = TreeNode(2)
        >>> root.right = TreeNode(7)
        >>> Solution().searchBST(root, 5)
        
    """