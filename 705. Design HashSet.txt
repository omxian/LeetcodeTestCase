"""
        >>> hashSet = MyHashSet()
        >>> hashSet.add(1)
        >>> hashSet.add(2)
        >>> hashSet.contains(1)
        True
        >>> hashSet.contains(3)
        False
        >>> hashSet.add(2)
        >>> hashSet.contains(2)
        True
        >>> hashSet.remove(2)
        >>> hashSet.contains(2)
        False
    """